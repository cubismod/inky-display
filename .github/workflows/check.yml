name: check

on:
  push:
    branches: ["main"]
  pull_request:


jobs:
  check:
    name: check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - name: Setup Task
        uses: illbjorn/setup-task@main
      - name: Invoke Task
        shell: bash
        run: task check

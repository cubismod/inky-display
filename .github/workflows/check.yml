name: check

on:
  push:
    branches: ["main"]
  pull_request:


jobs:
  check:
    name: check
    runs-on: ubuntu-latest
    steps:
      - name: Setup Task
        uses: illbjorn/setup-task@main

      - name: Invoke Task
        shell: bash
        run: task check
